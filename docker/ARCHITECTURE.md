# 🏗️ ARCHITECTURE DIAGRAM

Arquitetura completa da infraestrutura Docker NFC/NFV.

---

## 📐 Visão Geral

```
                                    INTERNET
                                        │
                                        │
                                ┌───────▼────────┐
                                │   FIREWALL     │
                                │  (UFW/iptables)│
                                │  Ports: 80,443 │
                                └───────┬────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                                       │
            ┌───────▼────────┐                   ┌─────────▼────────┐
            │  HTTP (Port 80)│                   │ HTTPS (Port 443) │
            └───────┬────────┘                   └─────────┬────────┘
                    │                                      │
                    └──────────────┬───────────────────────┘
                                   │
                           ┌───────▼────────┐
                           │     NGINX      │
                           │ Reverse Proxy  │
                           │ Load Balancer  │
                           │ Rate Limiting  │
                           └───────┬────────┘
                                   │
                                   │ [Bridge Network: 172.28.0.0/16]
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        │                          │                          │
┌───────▼────────┐        ┌────────▼───────┐        ┌────────▼───────┐
│   API:3000     │        │   API:3000     │        │   API:3000     │
│  (Replica 1)   │        │  (Replica 2)   │        │  (Replica 3)   │
│                │        │                │        │                │
│  - REST API    │        │  - REST API    │        │  - REST API    │
│  - Swagger     │        │  - Swagger     │        │  - Swagger     │
│  - Health      │        │  - Health      │        │  - Health      │
│  - Upload      │        │  - Upload      │        │  - Upload      │
└───────┬────────┘        └────────┬───────┘        └────────┬───────┘
        │                          │                          │
        └──────────────────────────┼──────────────────────────┘
                                   │
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        │                          │                          │
┌───────▼────────┐        ┌────────▼───────┐        ┌────────▼───────┐
│  PostgreSQL    │        │     Redis      │        │     MinIO      │
│   Port 5432    │        │   Port 6379    │        │  Port 9000/1   │
│                │        │                │        │                │
│  - Prisma ORM  │        │  - Cache       │        │  - S3 Storage  │
│  - Migrations  │        │  - BullMQ      │        │  - Videos      │
│  - Backups     │        │  - Sessions    │        │  - Thumbnails  │
│  - 16 Alpine   │        │  - 7 Alpine    │        │  - Results     │
└───────┬────────┘        └────────┬───────┘        └────────┬───────┘
        │                          │                          │
        │                          │                          │
        │                   ┌──────▼──────┐                  │
        │                   │             │                  │
        │            ┌──────▼──────┐ ┌────▼─────────┐       │
        │            │  Worker 1   │ │  Worker 2    │       │
        │            │  TF + FFmpeg│ │  TF + FFmpeg │       │
        │            └──────┬──────┘ └────┬─────────┘       │
        │                   │             │                  │
        │            ┌──────▼─────────────▼──────┐          │
        │            │       Worker 3             │          │
        │            │      TF + FFmpeg           │          │
        │            └──────┬─────────────────────┘          │
        │                   │                                │
        └───────────────────┴────────────────────────────────┘
                            │
                            │
                     ┌──────▼──────┐
                     │   Volumes   │
                     │             │
                     │ - postgres  │
                     │ - redis     │
                     │ - minio     │
                     │ - uploads   │
                     │ - frames    │
                     │ - results   │
                     └─────────────┘
```

---

## 🔄 Fluxo de Dados

### 1. Upload de Vídeo

```
User → Nginx → API → MinIO
              ↓
          PostgreSQL (metadata)
              ↓
          BullMQ Job → Redis Queue
                         ↓
                      Worker
                         ↓
                    ┌────┴────┐
                    ↓         ↓
                 FFmpeg   TensorFlow
                    ↓         ↓
                Frames    Análise
                    ↓         ↓
                    └────┬────┘
                         ↓
                  PostgreSQL (results)
                         ↓
                  MinIO (thumbnails)
```

### 2. Processamento Assíncrono

```
API enqueue job
    ↓
Redis BullMQ Queue
    ↓
Worker dequeue
    ↓
Download video (MinIO)
    ↓
Extract frames (FFmpeg)
    ↓
Analyze biomechanics (TensorFlow)
    ↓
Calculate metrics
    ↓
Save results (PostgreSQL)
    ↓
Upload thumbnails (MinIO)
    ↓
Update job status
    ↓
Notify API (via Redis PubSub)
```

### 3. Consulta de Análise

```
User → Nginx → API
              ↓
          PostgreSQL (query results)
              ↓
          MinIO (presigned URL for video)
              ↓
          Return JSON
```

---

## 🔐 Camadas de Segurança

```
┌─────────────────────────────────────────────┐
│ Layer 1: Network Firewall                  │
│ - UFW/iptables                              │
│ - Fail2Ban                                  │
│ - DDoS protection                           │
└─────────────────┬───────────────────────────┘
                  ↓
┌─────────────────────────────────────────────┐
│ Layer 2: Nginx                              │
│ - SSL/TLS                                   │
│ - Rate limiting                             │
│ - Request filtering                         │
└─────────────────┬───────────────────────────┘
                  ↓
┌─────────────────────────────────────────────┐
│ Layer 3: Application (API)                  │
│ - JWT authentication                        │
│ - Input validation                          │
│ - CORS                                      │
│ - File type validation                      │
└─────────────────┬───────────────────────────┘
                  ↓
┌─────────────────────────────────────────────┐
│ Layer 4: Docker Network Isolation           │
│ - Bridge network (internal only)            │
│ - No direct access to DB/Redis              │
└─────────────────┬───────────────────────────┘
                  ↓
┌─────────────────────────────────────────────┐
│ Layer 5: Container Security                 │
│ - Non-root users                            │
│ - Read-only filesystems                     │
│ - Resource limits                           │
└─────────────────┬───────────────────────────┘
                  ↓
┌─────────────────────────────────────────────┐
│ Layer 6: Data Security                      │
│ - Encrypted at rest (MinIO)                │
│ - Encrypted in transit (SSL/TLS)           │
│ - Password hashing (bcrypt)                │
│ - Secrets management                        │
└─────────────────────────────────────────────┘
```

---

## 📊 Escalabilidade

### Horizontal Scaling

```
┌─────────────────────────────────────────────┐
│              Load Balancer                  │
│         (Nginx least_conn)                  │
└─────┬───────────┬───────────┬───────────────┘
      │           │           │
      ▼           ▼           ▼
   API 1       API 2       API 3       ... API N
      │           │           │               │
      └───────────┴───────────┴───────────────┘
                      │
                      ▼
              Shared Resources
                (DB, Redis, MinIO)
```

### Worker Scaling

```
BullMQ Queue
    │
    ├──→ Worker 1 (2 concurrent jobs)
    ├──→ Worker 2 (2 concurrent jobs)
    ├──→ Worker 3 (2 concurrent jobs)
    └──→ Worker N (2 concurrent jobs)
```

### Database Scaling (Futuro)

```
PostgreSQL Primary (Write)
    │
    ├──→ Replica 1 (Read)
    ├──→ Replica 2 (Read)
    └──→ Replica N (Read)
```

---

## 💾 Data Flow

### Volumes e Persistência

```
┌─────────────────────────────────────────────┐
│          Docker Named Volumes                │
└─────────────────────────────────────────────┘
    │
    ├─→ postgres_data (10GB)
    │   └─→ /var/lib/postgresql/data
    │
    ├─→ redis_data (1GB)
    │   └─→ /data (AOF + RDB)
    │
    ├─→ minio_data (50GB)
    │   └─→ /data (S3 buckets)
    │
    ├─→ api_uploads (5GB)
    │   └─→ /app/data/uploads
    │
    ├─→ api_frames (10GB)
    │   └─→ /app/data/frames
    │
    ├─→ api_results (2GB)
    │   └─→ /app/data/results
    │
    ├─→ worker_frames (10GB)
    │   └─→ /app/data/frames
    │
    ├─→ worker_results (2GB)
    │   └─→ /app/data/results
    │
    └─→ nginx_logs (500MB)
        └─→ /var/log/nginx
```

### Backup Flow

```
┌─────────────────────────────────────────────┐
│        Scheduled Backup (cron 3AM)          │
└─────────────────┬───────────────────────────┘
                  │
        ┌─────────┴──────────┐
        │                    │
        ▼                    ▼
  PostgreSQL dump      Redis dump (RDB)
        │                    │
        └─────────┬──────────┘
                  │
                  ▼
          Compress (tar.gz)
                  │
                  ▼
      Local Storage (/backups)
                  │
                  ▼
         Optional: Cloud Sync
            (S3, GCS, etc)
```

---

## 🔄 High Availability Setup (Futuro)

```
                    Load Balancer (HAProxy)
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
    Server 1           Server 2           Server 3
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                  Shared Storage (NFS/GlusterFS)
                           │
              ┌────────────┼────────────┐
              │            │            │
              ▼            ▼            ▼
        PostgreSQL    Redis Cluster   MinIO
         Primary       (3 nodes)     Distributed
            +
        Replicas
```

---

## 📈 Monitoring Stack (Opcional)

```
┌─────────────────────────────────────────────┐
│              Prometheus                      │
│         (Metrics Collection)                 │
└─────┬───────────────────────────────────────┘
      │
      ├──→ Node Exporter (System metrics)
      ├──→ PostgreSQL Exporter
      ├──→ Redis Exporter
      ├──→ MinIO Metrics
      └──→ App Metrics (custom)
                  │
                  ▼
            ┌─────────┐
            │ Grafana │
            │Dashboard│
            └─────────┘
                  │
                  ▼
            ┌─────────┐
            │ Alerts  │
            │(Slack,  │
            │ Email)  │
            └─────────┘
```

---

## 🗺️ Network Topology

```
┌──────────────────────────────────────────────┐
│       Public Network (0.0.0.0/0)             │
│            ↕ Firewall ↕                      │
│         Ports: 80, 443, 22                   │
└──────────────────┬───────────────────────────┘
                   │
┌──────────────────▼───────────────────────────┐
│    Bridge Network (172.28.0.0/16)            │
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Nginx   │  │   API    │  │  Worker  │  │
│  │ .0.10    │  │ .0.20-22 │  │ .0.30-32 │  │
│  └──────────┘  └──────────┘  └──────────┘  │
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │PostgreSQL│  │  Redis   │  │  MinIO   │  │
│  │ .0.100   │  │ .0.110   │  │ .0.120   │  │
│  └──────────┘  └──────────┘  └──────────┘  │
│                                              │
│  No external access to DB, Redis, MinIO     │
│  Only accessible from within network         │
└──────────────────────────────────────────────┘
```

---

## 🔧 CI/CD Pipeline (Futuro)

```
┌─────────────────────────────────────────────┐
│           Developer Push Code               │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│         GitHub Actions Trigger              │
└─────────────────┬───────────────────────────┘
                  │
        ┌─────────┴──────────┐
        │                    │
        ▼                    ▼
   Run Tests           Build Docker Images
        │                    │
        └─────────┬──────────┘
                  │
                  ▼
            Push to Registry
          (Docker Hub, ECR)
                  │
                  ▼
┌─────────────────────────────────────────────┐
│         Deploy to Staging                   │
│    docker-compose pull && up -d             │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
         Integration Tests
                  │
                  ▼
┌─────────────────────────────────────────────┐
│         Manual Approval                     │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│      Deploy to Production                   │
│  Blue-Green or Rolling Update               │
└─────────────────────────────────────────────┘
```

---

## 📚 Tecnologias Usadas

| Componente | Tecnologia | Versão | Porta |
|------------|-----------|--------|-------|
| **Reverse Proxy** | Nginx | Alpine | 80, 443 |
| **API** | NestJS + Node | 20 Alpine | 3000 |
| **Worker** | Node + TensorFlow | 20 Alpine | - |
| **Database** | PostgreSQL | 16 Alpine | 5432 |
| **Cache/Queue** | Redis | 7 Alpine | 6379 |
| **Storage** | MinIO | Latest | 9000, 9001 |
| **ORM** | Prisma | Latest | - |
| **Queue** | BullMQ | Latest | - |
| **Video** | FFmpeg | Latest | - |
| **ML** | TensorFlow.js | Latest | - |

---

**Documentado por:** NutriFitCoach DevOps
**Versão:** 1.0.0
**Data:** 2026-02-15
